export function useTasks(pspId: string) {
  // Placeholder: returns tasks grouped by category and provides stub CRUD methods
  return {
    tasks: [],
    createTask: async (payload: any) => {},
    updateTask: async (id: string, patch: any) => {},
    deleteTask: async (id: string) => {}
  }
}
